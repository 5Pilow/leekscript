name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: pilow/leekscript-builder

    steps:
    - uses: actions/checkout@v2
    - name: make test
      run: make test
